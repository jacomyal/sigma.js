{
  "name": "knowledge-graph-app",
  "version": "1.0.0",
  "description": "Aplikacja do wizualizacji grafów wiedzy",
  "scripts": {
    "start": "node backend/api-server.js",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2"
  }
}
