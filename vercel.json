{
  "buildCommand": "npm install && cd packages/demo && npm install && npm run build",
  "outputDirectory": "packages/demo/build",
  "installCommand": "echo 'Installing only project dependencies'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
} 