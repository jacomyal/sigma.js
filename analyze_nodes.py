import json, math; data = json.load(open("frontend/public/sql_graph.json")); nodes = data.get("nodes", []); sizes = [(n.get("key"), n.get("size")) for n in nodes]; sizes.sort(key=lambda x: x[1], reverse=True); max_entity, max_size = sizes[0]; min_entity, min_size = sizes[-1]; print(f"Encja z największym rozmiarem: {max_entity}, rozmiar: {max_size}"); print(f"Encja z najmniejszym rozmiarem: {min_entity}, rozmiar: {min_size}"); print(f"Różnica w rozmiarze: {max_size - min_size}")
