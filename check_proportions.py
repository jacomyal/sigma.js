import json; data = json.load(open("frontend/public/sql_graph.json")); nodes = data.get("nodes", []); top_nodes = sorted(nodes, key=lambda n: n.get("size", 0), reverse=True)[:5]; max_size = top_nodes[0].get("size"); print("Top 5 encji z największym rozmiarem:"); [print(f"{node.get(\"key\")}: rozmiar = {node.get(\"size\")}, proporcja do największego = {node.get(\"size\")/max_size:.2f}") for node in top_nodes]
